<?xml version="1.0" encoding="utf-8"?>
<!--
  portage-recv (importer) — GrapheneOS device-parity transfer
  Copyright (C) 2026 Grepon Labs LLC. Licensed under AGPL-3.0.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Importer requests WRITE access to the Tier-0 domains (+ READ for dedup/matching). -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALL_LOG" />
    <!-- Camera scans the pairing QR (the out-of-band trust anchor). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Assisted reinstall fires per-app installer intents (Tier 0). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!--
      SMS restore (DEVILS_ADVOCATE.md Q4): holding ROLE_SMS requires declaring the SMS
      app components (SMS_DELIVER / WAP_PUSH_DELIVER receivers, etc.) AND a teardown that
      hands the role back to the recorded prior holder. Intentionally NOT wired here yet —
      treat SMS as its own mini-project with its own review.
    -->

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Portage">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
